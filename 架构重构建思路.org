#+TITLE: 架构重构建思路
* 构建思路
- 使用接收参数的构建器重构，而非事后更改构建函数。
- system模块负责提供构建system的方法。
- home模块负责提供构建home的方法。
- sets模块负责组织策略，提供预设的服务构建配置。
- user模块负责定义用户信息。
- loader模块负责加载配置。
- machine模块负责根据实际需求和机器情况拼接其它模块配置，输出完整的构建。
- 所有模块，原则上都尽量不操作别的模块，而是让machine来拼接。
